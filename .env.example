# =============================================================================
# TRADING BOT - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual API keys
# The .env file should NEVER be committed to git (already in .gitignore)

# =============================================================================
# BYBIT API CREDENTIALS (REQUIRED for live trading)
# =============================================================================
# Get your API keys from: https://www.bybit.com/app/user/api-management
BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here

# =============================================================================
# HYBRID AI SYSTEM (OPTIONAL - Rizzo Integration)
# =============================================================================
# These keys enable the AI-powered trading validation system
# The bot works WITHOUT these keys (pure XGBoost mode)

# ---- OpenAI API (for GPT-4o AI Validation) ----
# Get your key from: https://platform.openai.com/api-keys
# Cost: ~$0.02-0.05 per trading cycle (~$2-5/day with 15min cycles)
# Leave empty to disable AI validation
OPENAI_API_KEY=

# ---- CoinMarketCap API (for Fear & Greed Index) ----
# Get your key from: https://pro.coinmarketcap.com/account
# Free tier: 10,000 calls/month (sufficient for sentiment tracking)
# Leave empty to disable sentiment analysis
CMC_PRO_API_KEY=

# ---- Whale Alert API (for large transaction tracking) ----
# Get your key from: https://whale-alert.io/
# Requires paid subscription
# Leave empty to disable whale alerts
WHALE_ALERT_API_KEY=

# =============================================================================
# USAGE NOTES
# =============================================================================
# 1. AI_VALIDATION_ENABLED = False (default)
#    -> Bot uses only XGBoost ML (current behavior)
#    -> No AI costs, no OpenAI key needed
#
# 2. AI_VALIDATION_ENABLED = True + OPENAI_API_KEY set
#    -> Hybrid mode: XGBoost generates signals, GPT-4o validates them
#    -> Best accuracy, but costs ~$2-5/day
#
# 3. MARKET_INTELLIGENCE_ENABLED = True (optional, can work without AI)
#    -> Collects news, sentiment, forecasts for logging/analysis
#    -> Useful even without AI validation
#
# 4. All hybrid system features are optional and gracefully disabled if keys missing
